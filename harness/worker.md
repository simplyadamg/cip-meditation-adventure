You are implementing CIP Meditation Adventure in the current repository.

1. Read AGENTS.md, docs/design.md, harness/features.json, harness/progress.md and recent git history/status.
2. Run baseline npm run check. If it fails, repair the existing failure before adding features.
3. Choose one eligible feature in currentMilestone, prioritize research/asset integration/state-machine risks. Honor dependencies and the review gate. Do not begin full-neighborhood production.
4. Implement one coherent increment; preserve the original Blender source and user's reference files. Inspect Blender before writes and serialize all authoring. Use only permitted tools. Do not invoke this harness or spawn another Codex session.
5. Run relevant tests and browser checks. Append concise decisions, files, evidence, known limitations, and next action to progress.md. Update only feature status/evidence, never criteria. A feature is verified only when every criterion has evidence. A code-only check cannot verify visual quality.
6. Commit passing, coherent work. If required access is unavailable, leave the state reviewable and report blocked; do not bypass sandbox or use a different asset pipeline without user direction.
7. Return the specified JSON. Report ready_for_review only after current milestone requirements have verifiable evidence. The runner independently checks status; never manufacture completion.
